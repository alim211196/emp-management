import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#292929", // Replace with your primary color value
    },
  },
  typography: {
    fontWeightBold: 700,
  },
});

export default theme;
